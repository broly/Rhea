module render:render_resource;

import :pipeline_object;
import :pipeline_family;

std::shared_ptr<RenderResourceInstance> RenderResource::query_single(uint32_t instance_id)
{
    return query_single(instance_id);
}
